[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = "0.9.0"
serde = { version = "1.0", features = ["derive"] } # for player stat
tokio = { version = "1.0", features = ["full"] } # for multiplayers

